[ADMIN_README.md](https://github.com/user-attachments/files/32326421/ADMIN_README.md)
# لوحة تحكم الأدمن - Idna Gaming Card

تمت إضافة لوحة تحكم كاملة للموقع تتيح لك:

- **إدارة الطلبات**: عرض كل الطلبات، تغيير حالتها (قيد الانتظار / مكتمل / ملغي)، حذفها، والبحث والتصفية فيها، بالإضافة إلى إحصائيات سريعة (إجمالي الطلبات، طلبات اليوم، إلخ).
- **إدارة الأسعار والمنتجات**: تعديل أسعار أي بطاقة في أي قسم (iTunes، فري فاير، PUBG...)، إضافة أو حذف عناصر، وإضافة أو حذف أقسام كاملة — كل ذلك بدون لمس الكود، والتغييرات تظهر مباشرة في الموقع.

## رابط لوحة التحكم

بعد رفع المشروع، افتح:

```
https://idna-game-card.onrender.com/admin.html
```

## تسجيل الدخول لأول مرة

بيانات الدخول الافتراضية (**يجب تغييرها فورًا**):

- اسم المستخدم: `admin`
- كلمة المرور: `ChangeMe123!`

### كيفية تغيير بيانات الدخول (مهم جدًا)

اذهب إلى لوحة تحكم Render الخاصة بمشروعك:

1. افتح الخدمة (Service) على [render.com](https://dashboard.render.com)
2. اذهب إلى تبويب **Environment**
3. أضف متغيرين جديدين:
   - `ADMIN_USERNAME` = اسم المستخدم الذي تريده
   - `ADMIN_PASSWORD` = كلمة مرور قوية من اختيارك
4. احفظ، وسيقوم Render تلقائيًا بإعادة تشغيل الخدمة بالبيانات الجديدة

⚠️ إذا لم تقم بتعيين هذه المتغيرات، سيبقى الموقع يستخدم بيانات الدخول الافتراضية وهذا غير آمن.

## ما الذي تغيّر في المشروع؟

- `server.js`: أُضيفت مسارات جديدة لتسجيل الدخول، وإدارة الطلبات والمنتجات، محمية بجلسة دخول (Session) عبر كوكيز آمنة.
- `data/products.json`: تم نقل كل الأقسام والأسعار التي كانت مكتوبة داخل `index.html` إلى هذا الملف، بحيث يمكن تعديلها من لوحة التحكم مباشرة.
- `public/index.html`: أصبح الموقع يجلب الأقسام والأسعار من الخادم (`/api/products`) بدلاً من أن تكون مكتوبة بشكل ثابت. في حال تعذر الاتصال بالخادم لأي سبب، يعود الموقع تلقائيًا لعرض نسخة احتياطية مخزنة داخل الصفحة حتى لا يتعطل الموقع أبدًا.
- `public/admin.html`: صفحة لوحة التحكم الجديدة بالكامل.

## مهم: حتى لا تُفقد الأسعار بعد كل تحديث للموقع على Render

كانت هناك مشكلتان تمنعان حفظ الأسعار بشكل دائم، وتم إصلاحهما في هذه النسخة:

1. **خطأ في اسم الملف**: كان ملف الأسعار داخل المشروع اسمه `data/products` بدلاً من `data/products.json`، فكان الخادم لا يجده أبدًا ويستخدم بيانات افتراضية قديمة مدمجة في الكود. تم تصحيح الاسم الآن.
2. **تخزين Render غير دائم**: بشكل افتراضي، أي ملف يكتبه الخادم (مثل الأسعار بعد تعديلها من لوحة التحكم) يُحفظ على قرص "مؤقت" يُمسح بالكامل في كل مرة تقوم فيها بعمل Deploy جديد للمشروع (رفع كود جديد). أي تعديل أسعار قمت به من لوحة التحكم سيُفقد عند أول تحديث قادم للموقع، ما لم تفعّل قرصًا دائمًا.

هناك طريقتان لحل هذه المشكلة بشكل دائم — اختر واحدة:

### الخيار أ (الأسهل والموصى به): قاعدة بيانات Postgres

تم تجهيز المشروع بحيث إذا أضفت قاعدة بيانات Postgres، يحفظ فيها الأسعار والطلبات تلقائيًا بدلاً من الملفات، ولن تُفقد أبدًا مهما أعدت النشر.

1. من لوحة Render: **New +** → **PostgreSQL**، واختر اسمًا وأنشئها (خطة Free متاحة لعدد محدود من الأيام ثم تتحول لمدفوعة؛ أو اختر خطة مدفوعة من البداية لديمومة أطول).
2. بعد إنشائها، انسخ قيمة **Internal Database URL** من صفحة قاعدة البيانات.
3. اذهب إلى خدمة الموقع (Web Service) → تبويب **Environment** → أضف متغيرًا جديدًا:
   - `DATABASE_URL` = (القيمة التي نسختها)
4. احفظ، وسيُعاد تشغيل الخدمة تلقائيًا وتبدأ بالحفظ في قاعدة البيانات مباشرة (سترى في الـ Logs رسالة "متصل بقاعدة بيانات Postgres").

بهذا لا تحتاج أي قرص دائم، والخطة نفسها (Free للموقع + قاعدة بيانات) قد تكفي حسب احتياجك.

### الخيار ب: قرص دائم (Persistent Disk)

1. من لوحة Render، افتح الخدمة (Service) → تبويب **Disks** → **Add Disk**.
   - Name: أي اسم، مثلاً `data`
   - Mount Path: `/var/data`
   - Size: 1 GB تكفي
   (ملاحظة: هذه الميزة تتطلب خطة Starter المدفوعة للخدمة، وليست متوفرة على الخطة المجانية.)
2. اذهب إلى تبويب **Environment** وأضف متغيرًا جديدًا:
   - `DATA_DIR` = `/var/data`
3. احفظ؛ سيُعاد نشر الخدمة تلقائيًا.

إذا لم تُضف `DATABASE_URL` ولا `DATA_DIR`، سيستمر الموقع بالعمل وحفظ الأسعار بشكل طبيعي بين الطلبات، لكن التعديلات ستُفقد وتعود للوضع الافتراضي عند كل Deploy جديد أو إعادة تشغيل للخدمة.

## ملاحظة أمان

- نقطة `/api/orders` (GET) القديمة التي كانت تعرض كل الطلبات للعامة تمت إزالتها/استبدالها بمسار محمي `/api/admin/orders` يتطلب تسجيل الدخول، لحماية بيانات عملائك (الإيميلات وتفاصيل الطلبات).
- الجلسات تُخزَّن في ذاكرة الخادم وتنتهي تلقائيًا بعد 12 ساعة، أو عند الضغط على "تسجيل الخروج".
